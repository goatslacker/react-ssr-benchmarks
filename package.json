{
  "name": "react-benchmarks",
  "version": "1.0.0",
  "description": "Benchmarks comparing React's server rendering for versions 12--15",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "benchmark": "^2.1.1"
  }
}
