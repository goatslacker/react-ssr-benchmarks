{
  "name": "react-ssr-benchmarks",
  "version": "1.0.3",
  "description": "Benchmarks comparing React's server rendering for versions 12--15",
  "main": "index.js",
  "scripts": {
    "bench": "NODE_ENV=production node run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "benchmark": "^2.1.1"
  }
}
